<h1>Books</h1>

<h3>本のタイトル</h3>

<h3>感想</h3>

<%= @books.each do |book| %>

<%= link_to user_path(@user) do %>
<%= attachment_image_tag(@user, :profile_image_id, :fill, 40, 40, format:'jpeg', fallback:'no_image.jpg') %>
<% end %>

<%= link_to book_path do %><%= book.title %><% end %>

<%= book.body %>

<% end %>
